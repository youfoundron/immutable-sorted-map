import get from './get'
import has from './has'
import includes from './includes'
import first from './first'
import last from './last'

export default () => {
  it('#get', get)
  it('#has', has)
  it('#includes', includes)
  it('#first', first)
  it('#last', last)
}
